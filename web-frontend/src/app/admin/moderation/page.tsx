// Admin — moderation for the social layer (messages/follows). See /admin/plan.md §3.5.
// TODO: review reported content; hide/remove; block a user from messaging/following (audited).
export default function AdminModerationPage() {
  return (
    <section>
      <h1 style={{ margin: 0 }}>Moderation</h1>
      <p style={{ color: "#6b7280" }}>Reported messages/profiles + actions. Not implemented — see admin/plan.md §3.5.</p>
    </section>
  );
}
