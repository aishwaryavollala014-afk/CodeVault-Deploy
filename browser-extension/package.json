{
  "name": "browser-extension",
  "version": "0.1.0",
  "private": true,
  "description": "CodeVault browser extension (Path B v2) — capture accepted solutions and send to git-service /api/ingest",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "typecheck": "tsc --noEmit",
    "test": "vitest run",
    "test:watch": "vitest",
    "build:zip": "npm run build && cd dist && rm -f ../../web-frontend/public/codevault-extension.zip && zip -rq ../../web-frontend/public/codevault-extension.zip . && echo zipped"
  },
  "dependencies": {
    "webextension-polyfill": "^0.12.0"
  },
  "devDependencies": {
    "@crxjs/vite-plugin": "^2.0.0-beta.25",
    "@types/chrome": "^0.0.270",
    "@types/webextension-polyfill": "^0.12.1",
    "typescript": "^5.5.3",
    "vite": "^5.4.0",
    "vitest": "^4.1.10"
  }
}
